// Desk structure configuration for Sanity Studio
// Note: Structure tool requires client-side Sanity integration
// Using default desk structure based on schema types

export const structure = null;
